MULTICHAIN_HOSTNAME=masternode
MULTICHAIN_HOST=127.0.0.1
MULTICHAIN_CHAIN_NAME=dockerchain
MULTICHAIN_P2P_PORT=7447
MULTICHAIN_RPC_PORT=8000
MULTICHAIN_RPC_USER=multichainrpc
# MULTICHAIN_RPC_PASSWORD=REPLACE_WITH_STRONG_PASSWORD     # do NOT commit real passwords
# Or provide via file: MULTICHAIN_RPC_PASSWORD_FILE=/run/secrets/rpc_password

# Master node runtime configuration
RPC_USER=multichainrpc
# RPC_PASSWORD=REPLACE_WITH_STRONG_PASSWORD  # set in .env or via RPC_PASSWORD_FILE
RPC_ALLOWIP=127.0.0.1
RPC_PORT=8000

# Example remote connection override
# MULTICHAIN_HOST=your.node.ip.address
# MULTICHAIN_CHAIN_NAME=yourchain
# (Legacy alternative ports removed for clarity)
